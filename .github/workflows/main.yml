name: SonarQube Quality gate

on:
  push:
    branches:
      - main

jobs:
  sonarqube:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check SonarQube Quality Gate
      run: |
        STATUS=$(curl -s "http://localhost:9000/api/qualitygates/project_status?projectKey=sports" -u "sqp_d66ddb36826ffe7bc6a7a9dcdee95bad25d89b31":)
        echo "SonarQube Quality Gate Status: $STATUS"
        if [ "$STATUS" == '{"projectStatus":{"status":"ERROR"}}' ]; then
        echo "SonarQube Quality Gate Failed"
        gh pr review --approve --body "SonarQube Quality Gate Failed" --event "COMMENT"
        exit 1
        fi
      continue-on-error: true

